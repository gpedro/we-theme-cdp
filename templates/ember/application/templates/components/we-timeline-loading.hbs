<div class="loading-more-content text-center">
  {{#if loadingMore}}
    {{t 'Carregando'}}... <img src="/imgs/loading.gif">
  {{else}}
    {{#if haveMore}}
      <a href='#' {{action 'getMore'}}>{{t 'Ver mais'}}</a>
    {{else}}
      {{t 'Ainda não existe conteúdo'}}
    {{/if}}
  {{/if}}
</div>
