<div class="loading-more-content text-center">
  {{#if loadingMore}}
    Carregando ... <img src="/core/images/loading.gif">
  {{else}}
    {{#if haveMore}}
      <a href='#' {{action 'getMore'}}>{{t 'Ver mais'}}</a>
    {{else}}
      Fim da lista.
    {{/if}}
  {{/if}}
</div>
