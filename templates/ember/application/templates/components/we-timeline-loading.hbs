<div class="loading-more-content text-center">
  {{#if loadingMore}}
    Carregando ... <img src="/core/images/loading.gif">
  {{else}}
    {{#if haveMore}}
      <a href='javascript:;' {{action 'getMore'}}>Ver mais</a>
    {{else}}
      Fim da lista.
    {{/if}}
  {{/if}}
</div>

|{{haveMore}}|