<div class="messenger-area-wrapper">
  <div class="messenger-column">
    <div class="messenger-area">
      {{#if isListOpen}}
        <div class="messenger">
          <div class="header" {{action 'closeList'}}>
            {{t 'Messenger'}}
          <button type="button" class="remove btn btn-default btn-xs pull-right">
            <div class="icon icon-star-full"></div>
          </button>
          </div>
          <ul class="nav nav-list friendlist contact-list">
            {{#each contacts}}
              {{render 'messenger/contact' this}}
            {{/each}}
          </ul>
          <ul class="nav nav-list rooms-list contact-list">
            <li class="global-room">
              <a {{action 'openPublicBox'}} >
                {{t 'Chat público'}}
              </a>
            </li>
          </ul>
          <div class="search-area">
            <input type="search" class="form-control" placeholder="Buscar" style="margin: 0 0 10px -7px; background: #f2f2f2; border-color: #f2f2f2;">
          </div>
        </div>
      {{else}}
        <div class="messenger-off" {{action 'openList'}}>
          <span class="ui-icon ui-icon-person"></span>
          <span class="text">{{t 'Messenger'}}</span>
          <button type="button" class="remove btn btn-default btn-xs pull-right">
            <div class="icon icon-star-open"></div>
          </button>
        </div>
      {{/if}}
    </div>
  </div>

  {{#each openContacts}}
    {{we-messenger-box contact=this}}
  {{/each}}

  {{we-messenger-public-box }}

</div>
